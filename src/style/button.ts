import { SxProps } from "@mui/material"
import { colors } from "./colors"

export const buttonStyle: SxProps = {
    "&:active": {
        backgroundColor: colors.button, // Substitua 'cor_desejada' pela cor desejada
    },
}
